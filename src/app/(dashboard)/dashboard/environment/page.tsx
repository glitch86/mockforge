import Section from '@/components/shared/Section';
import React from 'react';

const Environments = () => {
    return (
        <Section>
            env
        </Section>
    );
};

export default Environments;