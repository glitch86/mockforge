import Section from '@/components/shared/Section';
import React from 'react';

const Log = () => {
    return (
        <Section>
            activity log
        </Section>
    );
};

export default Log;