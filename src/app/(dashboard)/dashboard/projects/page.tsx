import Section from '@/components/shared/Section';
import React from 'react';

const Projects = () => {
    return (
        <div>
            projects
        </div>
    );
};

export default Projects;