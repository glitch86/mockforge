import Section from '@/components/shared/Section';
import React from 'react';

const Projects = () => {
    return (
        <Section>
            projects
        </Section>
    );
};

export default Projects;