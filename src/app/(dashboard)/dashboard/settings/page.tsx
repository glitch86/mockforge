import Section from '@/components/shared/Section';
import React from 'react';

const Settings = () => {
    return (
        <Section>
            settings
        </Section>
    );
};

export default Settings;